
bot:
  # token: can be used as env-var
  # guild: something guild :|
  setup-commands: false
  global-commands: false
  use-mobile-os: true
  debug-log: false

presence-updater:
  enabled: false
  delay: 10s
  presences:
    - state: Online now!
      type: custom
      status: online

    # - name:
    #   type:
    #   status:
    #   url:
    #   state:
